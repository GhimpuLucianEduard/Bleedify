<Window x:Class="BleedifyPersonal.Views.ManageComponenteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BleedifyPersonal.Views"
        mc:Ignorable="d"
        Title="ManageComponenteView" Height="750" Width="1100">
    <Grid HorizontalAlignment="Left" Width="1092">
        <DataGrid AutoGenerateColumns="False" x:Name="DonationDataGrid" SelectedItem="{Binding SelectedComponenta, Mode=TwoWay}" ItemsSource="{Binding Componente}" HorizontalAlignment="Left" Height="420" Margin="65,48,0,0" VerticalAlignment="Top" Width="671">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nume Donator" Binding="{Binding Donatie.Donator.Nume}"/>
                <DataGridTextColumn Header="Prenume Donator" Binding="{Binding Donatie.Donator.Prenume}"/>
                <DataGridTextColumn Header="TipComponenta" Binding="{Binding TipComponenta}"/>
                <DataGridTextColumn Header="Data" Binding="{Binding DataDepunere}"/>
                <DataGridTextColumn Header="Nume Pacient" Binding="{Binding Pacient.Nume}"/>
                <DataGridTextColumn Header="Prenume Pacient" Binding="{Binding Pacient.Prenume}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Command="{Binding DeleteDonatieCommand}" Content="Delete" HorizontalAlignment="Left" Margin="65,500,0,0" VerticalAlignment="Top" Width="185" Height="62"/>
        <Button Command="{Binding UpdateCommand}" Content="Modifica" HorizontalAlignment="Left" Margin="551,500,0,0" VerticalAlignment="Top" Width="185" Height="62"/>
    </Grid>
</Window>
